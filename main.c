#include <stdio.h>

int main() {
    printf("You have to run each program separately! Please run this in a test environment (like an isolated virtual machine). The code might access memory outside its scope and overwrite it, which might lead to unknown outcomes in the system.\n");
    return 0;
}
